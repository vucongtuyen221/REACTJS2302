import classNames from 'classnames/bind';
import styles from './Pop.module.scss'

const cx = classNames.bind(styles)
function Wraper({children}: {children:any}) {
  return (
    <div>Wraper</div>
  )
}

export default Wraper